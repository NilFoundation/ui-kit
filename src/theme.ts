import { createTheme } from "baseui";

/** this code is example. */
export const theme = createTheme(
  {},
  {
    colors: {
      primary: "#000",
      accent: "#fff",
      accent50: "#fff",
    },
  }
);
