import { expandProperty } from "inline-style-expand-shorthand";

export const BorderRadiusStyles = expandProperty("borderRadius", "0");

export const WithoutBorderStyles = expandProperty("border", "none");
