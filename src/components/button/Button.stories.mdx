import { Canvas, Meta, Story, ArgsTable, Source } from "@storybook/blocks";
import Button from "./Button";
import { BUTTON_SIZE } from "./types";
import StoriesLayout from "../../shared/ui/stories-layout";
import { HeartIcon } from "../icons";
import { getFigmaParameters } from "../../shared/ui/getFigmaParameters";

<Meta title="Form/Button" component={Button} />

export const Template = ({ children, ...args }) => {
  return (
    <StoriesLayout color="#141414">
      <Button size={BUTTON_SIZE.mini} {...args}>
        {children ?? "Mini Button"}
      </Button>
      <Button size={BUTTON_SIZE.compact} {...args}>
        {children ?? "Compact Button"}
      </Button>
      <Button size={BUTTON_SIZE.default} {...args}>
        {children ?? "Default Button"}
      </Button>
      <Button size={BUTTON_SIZE.large} {...args}>
        {children ?? "Large Button"}
      </Button>
    </StoriesLayout>
  );
};

# Button

<Canvas isColumn>
  <Story
    name="Primary"
    args={{ kind: "primary" }}
    parameters={getFigmaParameters(
      "https://www.figma.com/file/YjE625ScDMf2ILjWB1sTMc/System?node-id=1562%3A9551&mode=dev"
    )}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Secondary"
    args={{ kind: "secondary" }}
    parameters={getFigmaParameters(
      "https://www.figma.com/file/YjE625ScDMf2ILjWB1sTMc/System?node-id=1562%3A9745&mode=dev"
    )}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Tertiary"
    args={{ kind: "tertiary" }}
    parameters={getFigmaParameters(
      "https://www.figma.com/file/YjE625ScDMf2ILjWB1sTMc/System?node-id=1562%3A9939&mode=dev"
    )}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Danger"
    args={{ kind: "danger" }}
    parameters={getFigmaParameters(
      "https://www.figma.com/file/YjE625ScDMf2ILjWB1sTMc/System?node-id=1562%3A10302&mode=dev"
    )}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Toggle"
    args={{ kind: "toggle" }}
    parameters={getFigmaParameters(
      "https://www.figma.com/file/YjE625ScDMf2ILjWB1sTMc/System?node-id=1403%3A40014&mode=dev"
    )}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Text"
    args={{ kind: "text" }}
    parameters={getFigmaParameters(
      "https://www.figma.com/file/YjE625ScDMf2ILjWB1sTMc/System?node-id=1562%3A10133&mode=dev"
    )}
  >
    {Template.bind({})}
  </Story>
  <Story name="Disabled" args={{ disabled: true }}>
    {Template.bind({})}
  </Story>
  <Story name="Loading" args={{ isLoading: true, children: "" }}>
    {Template.bind({})}
  </Story>
  <Story name="With Start Enhancer" args={{ startEnhancer: () => <HeartIcon /> }}>
    {Template.bind({})}
  </Story>
  <Story name="With End Enhancer" args={{ endEnhancer: <HeartIcon /> }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Button} />

### Usage:

To use, import the component `Button` and from `@nilfoundation/ui-kit`.

<Source
  language="tsx"
  dark
  format={true}
  code={`
        import {Button, BUTTON_SIZE, BUTTON_KIND Upload} from "@nilfoundation/ui-kit";
         ...
        <Button>Button Label</Button>
        // With size
        <Button size={BUTTON_SIZE.mini}>Button Label</Button>
        // With kind
        <Button kind={BUTTON_KIND.gray}>Button Label</Button>
        // Disabled
        <Button disabled>Button Label</Button>
        // Loading
        <Button isLoading>Button Label</Button>
        // With enhancer
        <Button startEnhancer={<Upload />} endEnhancer={<Upload />}>Button Label</Button>
  `}
/>
