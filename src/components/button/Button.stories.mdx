import {Canvas, Meta, Story, ArgsTable, Source} from "@storybook/addon-docs/blocks";
import Button, { BUTTON_SHAPE, BUTTON_SIZE } from "./Button";
import StoriesLayout from "../../shared/ui/stories-layout";
import {  Upload } from "baseui/icon";

<Meta
    title="Button"
    component={Button}
/>

export const Template = ({children, ...args}) => {
    return (
      <StoriesLayout color="#141414">
        <Button size={BUTTON_SIZE.mini} {...args}>{children ?? "Mini Button"}</Button>
        <Button size={BUTTON_SIZE.compact} {...args}>{children ?? "Compact Button"}</Button>
        <Button size={BUTTON_SIZE.default} {...args}>{children ?? "Default Button"}</Button>
        <Button size={BUTTON_SIZE.large} {...args}>{children ?? "Large Button"}</Button>
      </StoriesLayout>
    );
}

# Button


<Canvas isColumn>
    <Story name="Primary" args={{kind: "primary"}}>
        {Template.bind({})}
    </Story>
    <Story name="Secondary" args={{kind: "secondary"}}>
        {Template.bind({})}
    </Story>
    <Story name="Tertiary" args={{kind: "tertiary"}}>
        {Template.bind({})}
    </Story>
    <Story name="Disabled" args={{disabled: true}}>
        {Template.bind({})}
    </Story>
    <Story name="Loading" args={{isLoading: true}}>
        {Template.bind({})}
    </Story>
    <Story name="Shape Square" args={{shape: BUTTON_SHAPE.square, children: <Upload size="24px" />}}>
        {Template.bind({})}
    </Story>
    <Story name="With Start Enhancer" args={{startEnhancer: <Upload size="20px" />}}>
        {Template.bind({})}
    </Story>
  <Story name="With End Enhancer" args={{endEnhancer: <Upload size="20px" />}}>
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable of={Button} />

### Usage:

To use, import the component `Button` and from `@nilfoundation/ui-kit`.

<Source
  language='tsx'
  dark
  format={true}
  code={`
        import {Button, BUTTON_SIZE, BUTTON_SHAPE, Upload} from "@nilfoundation/ui-kit";
         ...
        <Button>Button Label</Button>
        // With size
        <Button size={BUTTON_SIZE.mini}>Button Label</Button>
        // With kind
        <Button kind={"primary"}>Button Label</Button>
        // With shape
        <Button shape={BUTTON_SHAPE.square}>Button Label</Button>
        // Disabled
        <Button disabled>Button Label</Button>
        // Loading
        <Button isLoading>Button Label</Button>
        // With enhancer
        <Button startEnhancer={<Upload />} endEnhancer={<Upload />}>Button Label</Button>
  `}
/>
