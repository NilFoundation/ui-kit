export enum BUTTON_KIND {
  primary = "primary",
  secondary = "secondary",
  tertiary = "tertiary",
  danger = "danger",
}

export enum BUTTON_SIZE {
  mini = "mini",
  default = "default",
  compact = "compact",
  large = "large",
}

export enum BUTTON_SHAPE {
  default = "default",
  square = "square",
}
