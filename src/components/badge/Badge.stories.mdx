import {Canvas, Meta, Story, ArgsTable} from "@storybook/addon-docs/blocks";
import Badge, {SHAPE, COLOR} from "./Badge";
import {Skeleton} from "../skeleton";

<Meta
    title="Badge"
    component={Badge}
/>

export const Template = ({...args}) => {
    return (
      <Badge {...args} />
    );
}


export const content = "Badge"

# Badge


<Canvas isColumn>
  <Story name="Base" args={{ content }}>
    {Template.bind({})}
  </Story>
  <Story name="Secondary" args={{ content, hierarchy: "secondary" }}>
    {Template.bind({})}
  </Story>
  <Story name="Pill Shape" args={{ content, shape: SHAPE.pill }}>
    {Template.bind({})}
  </Story>
  <Story name="With Color" args={{ content, color: COLOR.positive }}>
    {Template.bind({})}
  </Story>
  <Story
    name="With Children"
    args={{
      content,
      color: COLOR.positive,
      children: <Skeleton animation width="180px" height="120px" />
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Badge} />

### Использование:

