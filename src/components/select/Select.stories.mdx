import {Canvas, Meta, Story, ArgsTable} from "@storybook/addon-docs/blocks";
import Select from "./Select";
import SelectStoryWrapper from "./ui/SelectStoryWrapper";

<Meta
    title="Dropdown/Select"
    component={Select}
/>

export const Template = ({...args}) => {
    return (
      <SelectStoryWrapper>
        <Select {...args} />
      </SelectStoryWrapper>
    );
}

export const options = [
  { label: "AliceBlue", id: "#F0F8FF" },
  { label: "AntiqueWhite", id: "#FAEBD7" },
  { label: "Aqua", id: "#00FFFF" },
  { label: "Aquamarine", id: "#7FFFD4" },
  { label: "Azure", id: "#F0FFFF" },
  { label: "Beige", id: "#F5F5DC" },
];

# Select


<Canvas>
  <Story name="Base" args={{ options, placeholder: "Base Select" }}>
    {Template.bind({})}
  </Story>
  <Story name="Multiple" args={{ options, placeholder: "Multiple Select", multi: true }}>
    {Template.bind({})}
  </Story>
  <Story name="Error" args={{ options, placeholder: "Error Select", error: true }}>
    {Template.bind({})}
  </Story>
  <Story name="Positive" args={{ options, placeholder: "Positive Select", positive: true }}>
    {Template.bind({})}
  </Story>
  <Story name="Without Searchable" args={{ options, placeholder: "Searchable Select", searchable: false }}>
    {Template.bind({})}
  </Story>
  <Story name="Without Default Value" args={{ options, placeholder: "With Default Value", value: [options[0]] }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Select} />

### Использование:

