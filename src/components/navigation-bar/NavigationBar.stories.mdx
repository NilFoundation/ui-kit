import {Canvas, Meta, Story, ArgsTable, Source} from "@storybook/addon-docs/blocks";
import NavigationBar from "./NavigationBar";
import AuthDropdownContainer from "./AuthDropdownContainer";
import AuthItem from "./AuthItem";
import {Brand} from "../brand";

<Meta
    title="NavigationBar"
    component={NavigationBar}
/>

export const Template = ({...args}) => {
    return (
      <div style={{width: "100%", height: "240px", transform: "scale(1)"}}>
        <NavigationBar {...args} />
      </div>
    );
}

export const items = [
  {
    id: "1",
    label: "Menu item 1",
    isSelected: true
  },
  {
    id: "2",
    label: "Menu item 2",
    children: [
      {
        id: "4",
        label: "Menu subitem 1",
        disabled: true,
      },
      {
        id: "5",
        label: "Menu subitem 2",
      },
      {
        id: "6",
        label: "Menu subitem 3",
      }
    ]
  },
  {
    id: "3",
    label: "Menu item 3",
    disabled: true,
  },
]

export const authDropdownContainer = (
  <AuthDropdownContainer username="John">
    <AuthItem amount="1069" cents="34" currency="USD" />
  </AuthDropdownContainer>
)

# NavigationBar


<Canvas isColumn>
  <Story name="Base" args={{items, username: "John", isAuth: false, authDropdownContainer, brand: <Brand />}}>
    {Template.bind({})}
  </Story>
  <Story name="Auth" args={{items, username: "John", isAuth: true, authDropdownContainer, brand: <Brand />}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={NavigationBar} />

### Usage:

To use, import the component `NavigationBar` and from `@nilfoundation/ui-kit`.

<Source
  language='tsx'
  dark
  format={true}
  code={`
        import {NavigationBar, Brand, AuthDropdownContainer, AuthItem} from "@nilfoundation/ui-kit";
        ...
        export const items = [
          {
            id: "1",
            label: "Menu item 1",
            isSelected: true
          },
          {
          id: "2",
          label: "Menu item 2",
          children: [
              {
              id: "3",
              label: "Menu subitem 1"
              }
            ]
          }
        ]
        ...
        <NavigationBar
            items={items}
            username="John"
            isAuth
            brand={<Brand />}
            authDropdownContainer={() => (
              <AuthDropdownContainer username="John">
                <AuthItem amount="1069" cents="34" currency="USD" />
              </AuthDropdownContainer>
            )}
        />
  `}
/>
