import { Canvas, Meta, Story, ArgsTable, Source } from "@storybook/blocks";
import StatefulTooltip from "./StatefulTooltip";
import Tooltip from "./Tooltip";
import { useStyletron } from "baseui";
import { Button, BUTTON_KIND, BUTTON_SIZE } from "../button";

<Meta title="Overlay/Tooltip" component={StatefulTooltip} />

export const Template = ({ ...args }) => {
  const [css] = useStyletron();
  return (
    <div
      className={css({
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        width: "100%",
        listStyle: "none",
        padding: "0",
      })}
      {...args}
    />
  );
};

# Tooltip

<Canvas isColumn>
  <Story name="On hover tooltip" args={{}}>
    <Template>
      <StatefulTooltip showArrow content="Base tooltip on hover">
        <Button kind={BUTTON_KIND.secondary} size={BUTTON_SIZE.compact}>
          Hover me
        </Button>
      </StatefulTooltip>
    </Template>
  </Story>
  <Story name="On click tooltip" args={{}}>
    <Template>
      <StatefulTooltip showArrow content="Base tooltip on click" triggerType="click">
        <Button kind={BUTTON_KIND.secondary} size={BUTTON_SIZE.compact}>
          Click me
        </Button>
      </StatefulTooltip>
    </Template>
  </Story>
  <Story name="Tooltip without arrow" args={{}}>
    <Template>
      <StatefulTooltip showArrow={false} content="Base tooltip without arrow">
        <Button kind={BUTTON_KIND.secondary} size={BUTTON_SIZE.compact}>
          Hover me
        </Button>
      </StatefulTooltip>
    </Template>
  </Story>
  <Story name="Tooltip with placement" args={{}}>
    <Template>
      <StatefulTooltip showArrow content="Base tooltip with placement" placement="right">
        <Button kind={BUTTON_KIND.secondary} size={BUTTON_SIZE.compact}>
          Hover me
        </Button>
      </StatefulTooltip>
    </Template>
  </Story>
  <Story name="Tooltip with long text" args={{}}>
    <Template>
      <StatefulTooltip
        showArrow
        content="Long, long text. Long, long text. Long, long text. Long, long text. Long, long text. Long, long text. Long, long text."
        placement="right"
      >
        <Button kind={BUTTON_KIND.secondary} size={BUTTON_SIZE.compact}>
          Hover me for long text
        </Button>
      </StatefulTooltip>
    </Template>
  </Story>
</Canvas>

### StatefulTooltip props

<ArgsTable of={StatefulTooltip} />

### Tooltip props

<ArgsTable of={Tooltip} />

### Usage:

To use, import the component `Tooltip` or `StatefulTooltip` from `@nilfoundation/ui-kit`.

<Source
  language="tsx"
  dark
  format={true}
  code={`
        import {StatefulTooltip, Tooltip} from "@nilfoundation/ui-kit";
        ...
        <StatefulTooltip content="StatefulTooltip content" showArrow>
          <span>
            Hover me
          </span>
        </StatefulTooltip>
        // Tooltip on the left side of the button
        <StatefulTooltip content="StatefulTooltip content" showArrow placement="left">
            ...
        </StatefulTooltip>
        // Tooltip's triggering event is click
        <StatefulTooltip content="StatefulTooltip content" triggerType="click" showArrow>
            ...
        </StatefulTooltip>
        // Tooltip usage
        <Tooltip content="Tooltip content" showArrow isOpen>
            ...
        </Tooltip>
      `}
/>
