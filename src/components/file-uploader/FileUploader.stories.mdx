import {Canvas, Meta, Story, ArgsTable} from "@storybook/addon-docs/blocks";
import FileUploader from "./FileUploader";

<Meta
    title="Field/FileUploader"
    component={FileUploader}
/>

export const Template = ({...args}) => {
    return (
      <FileUploader {...args} />
    );
}

# File Uploader


<Canvas>
  <Story name="Base" args={{}}>
      {Template.bind({})}
  </Story>
  <Story name="Disabled" args={{disabled: true}}>
      {Template.bind({})}
  </Story>
  <Story name="Multiple" args={{multiple: false}}>
      {Template.bind({})}
  </Story>
  <Story name="Error" args={{errorMessage: "Upload failed... connection was lost."}}>
      {Template.bind({})}
  </Story>
  <Story name="Progress" args={{progressAmount: 64, progressMessage: "Uploading... hang tight."}}>
    {Template.bind({})}
  </Story>
  <Story name="With Unknown Progress" args={{progressMessage: "Uploading... hang tight."}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={FileUploader} />

### Использование:

