import { Canvas, Meta, Story, ArgsTable, Source } from "@storybook/addon-docs/blocks";
import Skeleton from "./Skeleton";

<Meta title="Feedback/Skeleton" component={Skeleton} />

export const Template = ({ ...args }) => {
  return <Skeleton {...args} />;
};

export const WIDTH = "414px";
export const HEIGHT = "414px";

# Skeleton

<Canvas isColumn>
  <Story name="Base" args={{ width: WIDTH, height: HEIGHT }}>
    {Template.bind({})}
  </Story>
  <Story name="With Animation" args={{ width: WIDTH, height: HEIGHT, animation: true }}>
    {Template.bind({})}
  </Story>
  <Story name="With Rows" args={{ width: WIDTH, height: HEIGHT, animation: true, rows: 3 }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Skeleton} />

### Usage:

To use, import the component `Skeleton` from `@nilfoundation/ui-kit`.

<Source
  language="tsx"
  dark
  format={true}
  code={`
        import {Skeleton} from "@nilfoundation/ui-kit";
        const WIDTH = "400px";
        const HEIGHT = "400px";
        ...
        <Skeleton width={WIDTH} height={HEIGHT} />
        // With animation
        <Skeleton width={WIDTH} height={HEIGHT} animation />
        // With rows
        <Skeleton width={WIDTH} height={HEIGHT} animation rows={4} />
  `}
/>
