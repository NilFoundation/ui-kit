import {Canvas, Meta, Story, ArgsTable} from "@storybook/addon-docs/blocks";
import Skeleton from "./Skeleton";

<Meta
    title="Skeleton"
    component={Skeleton}
/>

export const Template = ({...args}) => {
    return (
      <Skeleton {...args} />
    );
}

# Skeleton


<Canvas isColumn>
  <Story name="Base" args={{}}>
    {Template.bind({})}
  </Story>
  <Story name="With Animation" args={{animation: true}}>
    {Template.bind({})}
  </Story>
  <Story name="With Rows" args={{animation: true, rows: 3}}>
    {Template.bind({})}
  </Story>
  <Story name="With Custom Dimensions" args={{animation: true, width: "400px", height: "300px"}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Skeleton} />

### Использование:

