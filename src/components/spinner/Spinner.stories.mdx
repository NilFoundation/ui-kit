import { Canvas, Meta, Story, ArgsTable, Source } from "@storybook/blocks";
import Spinner from "./Spinner";
import { SPINNER_KIND, SPINNER_SIZE } from "./types";
import { COLORS } from "../../shared";

<Meta title="Feedback/Spinner" component={Spinner} />

export const Template = ({ ...args }) => {
  return (
    <div style={args.kind === SPINNER_KIND.dark ? { backgroundColor: COLORS.gray100, padding: "24px" } : {}}>
      <Spinner {...args} />
    </div>
  );
};

# Spinner

<Canvas isColumn>
  <Story name="XSmall" args={{ size: SPINNER_SIZE.xSmall }}>
    {Template.bind({})}
  </Story>
  <Story name="Small" args={{ size: SPINNER_SIZE.small }}>
    {Template.bind({})}
  </Story>
  <Story name="Medium" args={{ size: SPINNER_SIZE.medium }}>
    {Template.bind({})}
  </Story>
  <Story name="Large" args={{ size: SPINNER_SIZE.large }}>
    {Template.bind({})}
  </Story>
  <Story name="XLarge" args={{ size: SPINNER_SIZE.xLarge }}>
    {Template.bind({})}
  </Story>
  <Story name="With kind" args={{ kind: SPINNER_KIND.dark }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Spinner} />

### Usage:

To use, import the component `Spinner` from `@nilfoundation/ui-kit`.

<Source
  language="tsx"
  dark
  format={true}
  code={`
        import {Spinner, SPINNER_SIZE} from "@nilfoundation/ui-kit";
        ...
        <Spinner />
        // With size
        <Spinner size={SPINNER_SIZE.large} />
        // With kind
        <Spinner animation label="Uploading..." kind={SPINNER_KIND.dark} />
  `}
/>
