import { StyleObject } from "styletron-standard";

const containerStyles: StyleObject = {
  width: "100%",
  height: "100%",
  display: "flex",
  flexDirection: "column",
  flexGrow: 1,
};

export const styles = {
  containerStyles,
};
